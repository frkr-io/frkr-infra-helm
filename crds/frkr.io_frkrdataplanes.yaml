---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: frkrdataplanes.frkr.io
spec:
  group: frkr.io
  names:
    kind: FrkrDataPlane
    listKind: FrkrDataPlaneList
    plural: frkrdataplanes
    singular: frkrdataplane
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.postgresConnected
      name: Postgres
      type: string
    - jsonPath: .status.brokerConnected
      name: Broker
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: FrkrDataPlane is the Schema for the frkrdataplanes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FrkrDataPlaneSpec defines the desired state of FrkrDataPlane
            properties:
              brokerConfig:
                description: BrokerConfig is the Kafka-compatible message broker configuration
                properties:
                  brokers:
                    description: Brokers is a list of broker addresses
                    items:
                      type: string
                    type: array
                  tlsConfigRef:
                    description: TLSConfigRef is a reference to a secret containing
                      TLS certificates
                    type: string
                  tlsEnabled:
                    description: TLSEnabled indicates if TLS is enabled
                    type: boolean
                required:
                - brokers
                type: object
              postgresConfig:
                description: PostgresConfig is the PostgreSQL-compatible database
                  configuration
                properties:
                  database:
                    description: Database is the database name
                    type: string
                  host:
                    description: Host is the database host
                    type: string
                  passwordRef:
                    description: PasswordRef is a reference to a secret containing
                      the password
                    type: string
                  port:
                    description: Port is the database port
                    type: integer
                  sslMode:
                    description: SSLMode is the SSL mode (require, disable, etc.)
                    type: string
                  type:
                    default: postgres
                    description: Type is the database type (postgres, cockroachdb)
                    enum:
                    - postgres
                    - cockroachdb
                    type: string
                  user:
                    description: User is the database user
                    type: string
                required:
                - database
                - host
                - passwordRef
                - user
                type: object
            required:
            - brokerConfig
            - postgresConfig
            type: object
          status:
            description: FrkrDataPlaneStatus defines the observed state of FrkrDataPlane
            properties:
              brokerConnected:
                description: BrokerConnected indicates if Kafka-compatible broker
                  connection is healthy
                type: boolean
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase indicates the current phase
                type: string
              postgresConnected:
                description: PostgresConnected indicates if Postgres connection is
                  healthy
                type: boolean
              warnings:
                description: Warnings contains any connectivity warnings
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
