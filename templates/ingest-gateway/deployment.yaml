{{- if .Values.ingestGateway.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "frkr.fullname" . }}-ingest-gateway
  labels:
    {{- include "frkr.labels" . | nindent 4 }}
    app.kubernetes.io/component: ingest-gateway
spec:
  replicas: {{ .Values.ingestGateway.replicaCount }}
  selector:
    matchLabels:
      {{- include "frkr.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: ingest-gateway
  template:
    metadata:
      labels:
        {{- include "frkr.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: ingest-gateway
    spec:
      containers:
      - name: ingest-gateway
        image: "{{ .Values.global.imageRegistry }}{{ .Values.ingestGateway.image.repository }}:{{ .Values.ingestGateway.image.tag }}"
        imagePullPolicy: {{ .Values.ingestGateway.image.pullPolicy }}
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        env:
        - name: HTTP_PORT
          value: "8080"
        - name: AUTH_TYPE
          value: "{{ .Values.auth.type }}"
        - name: DB_URL
          value: "postgres://{{ .Values.dataPlane.db.user }}:{{ .Values.dataPlane.db.password }}@{{ .Values.dataPlane.db.host }}:{{ .Values.dataPlane.db.port }}/{{ .Values.dataPlane.db.database }}?sslmode={{ .Values.dataPlane.db.sslMode }}"
        {{- if .Values.dataPlane.db.password }}
        - name: DB_PASSWORD
          value: {{ .Values.dataPlane.db.password | quote }}
        {{- else if .Values.dataPlane.db.passwordRef }}
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.dataPlane.db.passwordRef }}
              key: password
        {{- end }}
        - name: BROKER_URL
          value: "{{ join "," .Values.dataPlane.broker.brokers }}"
        resources:
          {{- toYaml .Values.ingestGateway.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
{{- end }}

